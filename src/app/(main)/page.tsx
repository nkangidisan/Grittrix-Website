
/**
 * This is a placeholder page for the (main) route group.
 * It renders null and exists to satisfy Next.js's routing structure.
 * The actual homepage content is in `src/app/page.tsx`.
 */
export default function MainPage() {
  return null;
}
