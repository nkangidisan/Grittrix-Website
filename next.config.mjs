/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // No remotePatterns needed for local images in /public
    // but keeping the empty object for future-proofing
  },
};

export default nextConfig;
